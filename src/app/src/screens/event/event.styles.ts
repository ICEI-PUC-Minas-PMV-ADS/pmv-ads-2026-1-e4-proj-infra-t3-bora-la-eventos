import { StyleSheet } from "react-native";

export const EventStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    marginTop: 10
  },
});
